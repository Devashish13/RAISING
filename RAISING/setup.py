from setuptools import setup, find_packages

setup(
    name='hp_tuning',
    version='0.1',
    packages=find_packages(),
    install_requires=[
        # List your Python package dependencies here
    ],
)

